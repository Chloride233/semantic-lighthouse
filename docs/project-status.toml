# Semantic Lighthouse — Canonical Project Status
# This is the single source of truth for current product state.
# All entry documents MUST reference this file instead of maintaining
# their own "current phase" / "next phase" declarations.
# Schema version controls forward compatibility of check_doc_alignment.py.

[schema]
version = 1

[product]
phase = "Phase 19.2"
status = "delivered"
summary = "Phase 19.2 FDE Smoke Reads Manufacturing Data Pack Contract delivered: smoke_fde_demo.py accepts --data-pack flag, validates manifest (13 tables, core_pilot, PK/FK, row_count), prints manifest summary in recap. Auto-generates default tiny pack when flag omitted. 10 tests total (6 from 19.1 + 4 smoke data-pack tests). AdventureWorks remains a future external benchmark candidate, not in 19.1/19.2."

[frontend]
status = "F2 complete + visual baseline closeout accepted"
summary = "Guided Pilot workspace delivered. F2A: Pilot entry + nav restructure. F2B: Model -> Validate -> Pilot full operation loop. F2C: Responsive, accessibility, visual polish. Phase 15.3 Slice B adds evidence-to-draft proposal UI in goal stage. 2026-06-21/22 visual baseline refresh applies Handhold-inspired typography, spacing, pill CTAs, quiet cards, and Minimalism & Swiss Style while preserving Semantic Lighthouse color semantics. Legacy console pages now have screenshot QA coverage across desktop and mobile."
latest_verified_ui = "53/53"
latest_verified_e2e = "18/18"
screenshots_path = ".tmp/legacy-polish/"

[focus]
current = "Phase 19.2 delivered — FDE smoke reads data pack contract"
description = "smoke_fde_demo.py now validates and displays manufacturing data pack manifest as its input asset contract. Next: Phase 19.3 Mapping Contract v1 (offline/script-level, no database migration)."

[next_decision_gate]
name = "Phase 19.3: Mapping Contract v1"
description = "Implement Mapping Contract as a first-class offline/script-level validation artifact. Map source fields to business properties with value types, null strategies, and evidence sources. No database migration at this stage. AdventureWorks remains a candidate for a future external benchmark slice, not in 19.3."

[mcp]
runtime_status = "not started"
summary = "MCP is a future Agent-facing adapter candidate only. No MCP server/client, SDK, dependency, resource, or tool is implemented. Requires a dedicated Safety Lane plan after Phase 14 review."

[verification]
latest_commit = "3b6f776"
backend_pytest = 848
ruff = "clean"
migration_head = "0028"
