# Semantic Lighthouse — Canonical Project Status
# This is the single source of truth for current product state.
# All entry documents MUST reference this file instead of maintaining
# their own "current phase" / "next phase" declarations.
# Schema version controls forward compatibility of check_doc_alignment.py.

[schema]
version = 1

[product]
phase = "Phase 17"
status = "portfolio polish complete"
summary = "Phase 17 FDE Demo Readiness complete: seed scenario contract, 11-step smoke script, artifact quality gate, interview/demo script refresh, and portfolio entrypoint polish. Current strongest demo: smoke 11/11 PASS, artifact gate PASS (0 findings). Phase 16.3 UI remains deferred."

[frontend]
status = "F2 complete"
summary = "Guided Pilot workspace delivered. F2A: Pilot entry + nav restructure. F2B: Model → Validate → Pilot full operation loop. F2C: Responsive, accessibility, visual polish. Phase 15.3 Slice B adds evidence-to-draft proposal UI in goal stage."
latest_verified_ui = "53/53"
latest_verified_e2e = "18/18"
screenshots_path = ".tmp/f2c/"

[focus]
current = "Phase 18 planned"
description = "Phase 18 plan written: Cloud Deployment Smoke v1. Five slices: config audit (18.1), PostgreSQL migration smoke (18.2), HTTP health/API smoke (18.3), smoke adapter (18.4, may defer), closeout (18.5). Goal: prove FDE chain on PostgreSQL + Docker, not just local SQLite."

[next_decision_gate]
name = "implement Phase 18.1 config audit"
description = "Phase 18 plan written. First slice: 18.1 deployment config audit — review .env.example, docker-compose, Dockerfile, config.py, Alembic env.py. Doc-and-config-only (Fast Lane). See docs/phase18-planning.md."

[mcp]
runtime_status = "not started"
summary = "MCP is a future Agent-facing adapter candidate only. No MCP server/client, SDK, dependency, resource, or tool is implemented. Requires a dedicated Safety Lane plan after Phase 14 review."

[verification]
latest_commit = "d884813"
backend_pytest = 848
ruff = "clean"
migration_head = "0027"
