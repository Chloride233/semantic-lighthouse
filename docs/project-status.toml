# Semantic Lighthouse — Canonical Project Status
# This is the single source of truth for current product state.
# All entry documents MUST reference this file instead of maintaining
# their own "current phase" / "next phase" declarations.
# Schema version controls forward compatibility of check_doc_alignment.py.

[schema]
version = 1

[product]
phase = "Phase 18"
status = "closeout complete"
summary = "Phase 18 Cloud Deployment Smoke v1 complete: config audit (11 items), PostgreSQL migration smoke (28/28, migration 0028), HTTP API smoke (9/9 PASS), base-url adapter, closeout review (8 gates PASS). Proves FDE chain deployable on PostgreSQL + pgvector."

[frontend]
status = "F2 complete + visual baseline refreshed"
summary = "Guided Pilot workspace delivered. F2A: Pilot entry + nav restructure. F2B: Model -> Validate -> Pilot full operation loop. F2C: Responsive, accessibility, visual polish. Phase 15.3 Slice B adds evidence-to-draft proposal UI in goal stage. 2026-06-21 visual baseline refresh applies Handhold-inspired typography, spacing, pill CTAs, quiet cards, and Minimalism & Swiss Style while preserving Semantic Lighthouse color semantics."
latest_verified_ui = "53/53"
latest_verified_e2e = "18/18"
screenshots_path = ".tmp/f2c/"

[focus]
current = "frontend visual baseline refreshed"
description = "Frontend visual baseline refreshed: Handhold-inspired typography, spacing, pill CTAs, quiet cards, and responsive Pilot workspace screenshots verified under Minimalism & Swiss Style. Phase 18 closeout remains complete."

[next_decision_gate]
name = "continue UI polish or plan Phase 19"
description = "Frontend visual baseline is refreshed but intentionally first-pass. Next options: CC polish on remaining pages, 16.3 Outcome Panel UI, Phase 19 ontology operationalization with real data, or portfolio wrap-up. See docs/agent-handoff.md."

[mcp]
runtime_status = "not started"
summary = "MCP is a future Agent-facing adapter candidate only. No MCP server/client, SDK, dependency, resource, or tool is implemented. Requires a dedicated Safety Lane plan after Phase 14 review."

[verification]
latest_commit = "8b780e5"
backend_pytest = 848
ruff = "clean"
migration_head = "0028"
