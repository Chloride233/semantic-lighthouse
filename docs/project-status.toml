# Semantic Lighthouse - Canonical Project Status
# This is the single source of truth for current product state.
# All entry documents MUST reference this file instead of maintaining
# their own "current phase" / "next phase" declarations.
# Schema version controls forward compatibility of check_doc_alignment.py.

[schema]
version = 1

[product]
phase = "Phase 19"
status = "closeout complete"
summary = "Phase 19 Ontology Operationalization complete (19.1-19.6). Delivered the offline semantic chain and the AdventureWorks public benchmark through Semantic CI, governance decisions, accepted ontology drafts, model package, dataset binding, runtime query planning, temporary DB runtime query smoke, runtime audit, and acceptance feedback. All benchmark boundaries remain explicit: no application DB writes, no package publication, no runtime activation, and no enterprise human-review claim."

[frontend]
status = "F2 complete + visual baseline closeout accepted"
summary = "Guided Pilot workspace delivered. F2A: Pilot entry + nav restructure. F2B: Model -> Validate -> Pilot full operation loop. F2C: Responsive, accessibility, visual polish. Phase 15.3 Slice B adds evidence-to-draft proposal UI in goal stage. 2026-06-21/22 visual baseline refresh applies Handhold-inspired typography, spacing, pill CTAs, quiet cards, and Minimalism & Swiss Style while preserving Semantic Lighthouse color semantics. Legacy console pages now have screenshot QA coverage across desktop and mobile."
latest_verified_ui = "59/59"
latest_verified_e2e = "18/18"
screenshots_path = ".tmp/legacy-polish/"

[focus]
current = "DB runtime query smoke bridge delivered"
description = "Safety Lane slice advances the public benchmark chain beyond offline QUERY_PLANNED. Added run_db_runtime_query_smoke.py, which seeds temporary in-memory SQLite plus temporary dataset-storage, calls the existing runtime query service, reads bounded CSV rows, and verifies OntologyRuntimeAudit records without writing the application database, publishing packages, activating runtime, or creating real governance issues. AdventureWorks public benchmark fixture smoke executed 5 runtime queries, returned 70 rows, wrote 5 runtime audit records, rebuilt semantic feedback to NO_OPEN_FEEDBACK, and rebuilt acceptance status to DB_RUNTIME_SMOKE_PASS. Public benchmark fixture remains explicitly not enterprise human review."

[next_decision_gate]
name = "Runtime smoke complete / production promotion decision"
description = "The benchmark chain now proves temporary DB-backed runtime query execution and audit from offline artifacts. Next production-grade step is a separate Safety Lane decision: persist reviewed packages and dataset bindings into the real app database with explicit authorization, audit, rollback, and human approval semantics. Public benchmark fixture is sufficient for engineering validation, not for enterprise semantic approval."

[mcp]
runtime_status = "not started"
summary = "MCP is a future Agent-facing adapter candidate only. No MCP server/client, SDK, dependency, resource, or tool is implemented. Requires a dedicated Safety Lane plan after Phase 14 review."

[verification]
latest_commit = "8e21937"
backend_pytest = "full pytest timed out locally after 10 minutes; related semantic-loop tests passed"
ruff = "clean"
migration_head = "0029"
traverse_tests = 122
runtime_smoke_tests = 3
semantic_loop_tests = 25
mac_migration = "complete on M4 Mac mini arm64, verified 2026-07-12"
mac_python = "3.14.6 arm64"
mac_database = "PostgreSQL 17.10 with pgvector 0.8.5; Alembic 0029"
mac_migration_tests = "12/12 focused migration tests passed"
mac_adventureworks = "10 raw tables / 144593 rows; 13 semantic tables / 187508 rows; 25/25 recorded SHA references verified; runtime smoke 5 queries / 70 rows / 5 temporary audit records; application DB unchanged"
