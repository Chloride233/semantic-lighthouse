# Semantic Lighthouse — Canonical Project Status
# This is the single source of truth for current product state.
# All entry documents MUST reference this file instead of maintaining
# their own "current phase" / "next phase" declarations.
# Schema version controls forward compatibility of check_doc_alignment.py.

[schema]
version = 1

[product]
phase = "Phase 15"
status = "complete"
summary = "Evidence-to-Ontology Feedback Loop v1 — user-confirmed project evidence (15.1 save + 15.2 display) feeds into evidence-backed modeling draft candidates (15.3 endpoint + UI). Full loop: RAG answer → save as evidence → review evidence → propose draft from evidence → human review → existing package build. No LLM-only generation, no auto-accept, no external KB writes."

[frontend]
status = "F2 complete"
summary = "Guided Pilot workspace delivered. F2A: Pilot entry + nav restructure. F2B: Model → Validate → Pilot full operation loop. F2C: Responsive, accessibility, visual polish. Phase 15.3 Slice B adds evidence-to-draft proposal UI in goal stage."
latest_verified_ui = "53/53"
latest_verified_e2e = "18/18"
screenshots_path = ".tmp/f2c/"

[focus]
current = "Phase 15 complete"
description = "Phase 15 delivered: evidence-to-ontology feedback loop v1. 15.1 (save RAG answer as evidence), 15.2 (evidence review surface), 15.3 (evidence-backed draft candidate endpoint + UI). 22 evidence-draft tests + 53 UI tests pass. No migration, no MCP, no Graph RAG, no Agent auto-write."

[next_decision_gate]
name = "plan Phase 16"
description = "Phase 15 evidence-to-ontology feedback loop is complete. Next direction: enterprise Ontology semantic layer governance, ontology model operationalization, or Pilot workflow improvements. Decide based on product alignment PRD and interview/portfolio priorities."

[mcp]
runtime_status = "not started"
summary = "MCP is a future Agent-facing adapter candidate only. No MCP server/client, SDK, dependency, resource, or tool is implemented. Requires a dedicated Safety Lane plan after Phase 14 review."

[verification]
latest_commit = "b52e037"
backend_pytest = 848
ruff = "clean"
migration_head = "0026"
