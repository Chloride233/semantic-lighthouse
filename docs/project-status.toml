# Semantic Lighthouse — Canonical Project Status
# This is the single source of truth for current product state.
# All entry documents MUST reference this file instead of maintaining
# their own "current phase" / "next phase" declarations.
# Schema version controls forward compatibility of check_doc_alignment.py.

[schema]
version = 1

[product]
phase = "Phase 14"
status = "complete"
summary = "Business Pilot Project — five-stage main chain (goal → data → model → validate → pilot). Backend complete (Phases 14.1–14.5). Frontend F2 complete (F2A + F2B + F2C)."

[frontend]
status = "F2 complete"
summary = "Guided Pilot workspace delivered. F2A: Pilot entry + nav restructure. F2B: Model → Validate → Pilot full operation loop. F2C: Responsive, accessibility, visual polish."
latest_verified_ui = "53/53"
latest_verified_e2e = "18/18"
screenshots_path = ".tmp/f2c/"

[focus]
current = "S2.5B complete"
description = "S2.5B delivered: Pilot-stage task summary using existing project-scoped Tasks API. Shows compact task list with status badges, manual create-task action scoped to project. Uses GET /groups/{gid}/tasks?project_id={pid} and POST /groups/{gid}/tasks with project_id. S2.5A also complete. Next: S2.5C Scoped Conversation Starter."

[next_decision_gate]
name = "proceed to S2.5C or close S2.5"
description = "S2.5C adds lightweight scoped conversation starter from Goal or Pilot. S2.5D is navigation parity review (Fast/Standard). Consider closing S2.5 surface after S2.5C."

[mcp]
runtime_status = "not started"
summary = "MCP is a future Agent-facing adapter candidate only. No MCP server/client, SDK, dependency, resource, or tool is implemented. Requires a dedicated Safety Lane plan after Phase 14 review."

[verification]
latest_commit = "48eadcf"
backend_pytest = 848
ruff = "clean"
migration_head = "0026"
