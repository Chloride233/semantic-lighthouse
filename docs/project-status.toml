# Semantic Lighthouse — Canonical Project Status
# This is the single source of truth for current product state.
# All entry documents MUST reference this file instead of maintaining
# their own "current phase" / "next phase" declarations.
# Schema version controls forward compatibility of check_doc_alignment.py.

[schema]
version = 1

[product]
phase = "Phase 16"
status = "planned"
summary = "Pilot Outcome & FDE Delivery Record v1 — after a business Pilot reaches evidence/model/validate/pilot stages, produce a durable, auditable delivery record tying business goal → datasets → project evidence → modeling drafts/packages → runtime query findings → decision/outcome narrative. Plan created; implementation pending."

[frontend]
status = "F2 complete"
summary = "Guided Pilot workspace delivered. F2A: Pilot entry + nav restructure. F2B: Model → Validate → Pilot full operation loop. F2C: Responsive, accessibility, visual polish. Phase 15.3 Slice B adds evidence-to-draft proposal UI in goal stage."
latest_verified_ui = "53/53"
latest_verified_e2e = "18/18"
screenshots_path = ".tmp/f2c/"

[focus]
current = "Phase 16 planned"
description = "Phase 16 plan created: Pilot Outcome & FDE Delivery Record v1. Four slices designed (16.1 schema design, 16.2 outcome endpoint, 16.3 delivery report UI, 16.4 exportable interview artifact). Implementation pending. Hard boundaries: no Graph RAG, no MCP runtime, no Agent auto-write, no external KB write, no new framework."

[next_decision_gate]
name = "implement Phase 16.1 or review plan"
description = "Phase 16 plan is written. Next decision: implement Slice 16.1 (outcome record design/schema), review the full plan with project owner, or adjust scope based on interview/portfolio feedback. See docs/phase16-planning.md."

[mcp]
runtime_status = "not started"
summary = "MCP is a future Agent-facing adapter candidate only. No MCP server/client, SDK, dependency, resource, or tool is implemented. Requires a dedicated Safety Lane plan after Phase 14 review."

[verification]
latest_commit = "b52e037"
backend_pytest = 848
ruff = "clean"
migration_head = "0026"
