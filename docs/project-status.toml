# Semantic Lighthouse — Canonical Project Status
# This is the single source of truth for current product state.
# All entry documents MUST reference this file instead of maintaining
# their own "current phase" / "next phase" declarations.
# Schema version controls forward compatibility of check_doc_alignment.py.

[schema]
version = 1

[product]
phase = "Phase 18"
status = "planned"
summary = "Phase 18 Cloud Deployment Smoke v1 planned. Goal: prove the FDE demo chain works beyond local SQLite/TestClient through deployment config audit, PostgreSQL migration smoke, HTTP health/API smoke, optional FDE smoke adapter, and closeout. Current strongest completed demo remains Phase 17: smoke 11/11 PASS, artifact gate PASS (0 findings)."

[frontend]
status = "F2 complete"
summary = "Guided Pilot workspace delivered. F2A: Pilot entry + nav restructure. F2B: Model → Validate → Pilot full operation loop. F2C: Responsive, accessibility, visual polish. Phase 15.3 Slice B adds evidence-to-draft proposal UI in goal stage."
latest_verified_ui = "53/53"
latest_verified_e2e = "18/18"
screenshots_path = ".tmp/f2c/"

[focus]
current = "Phase 18.1 delivered"
description = "Phase 18.1 deployment config audit delivered: 11 items audited (8 PASS, 3 FIXED). Fixed .env.example (added APP_ENV, DATASET_STORAGE_PATH, fake provider comments, section headers). Updated cloud smoke playbook with FDE Chain 5."

[next_decision_gate]
name = "implement Phase 18.2 PostgreSQL migration smoke"
description = "Phase 18.1 delivered. Next: 18.2 — run alembic upgrade head on temporary PostgreSQL + pgvector, verify 0027 at head. See docs/phase18-planning.md."

[mcp]
runtime_status = "not started"
summary = "MCP is a future Agent-facing adapter candidate only. No MCP server/client, SDK, dependency, resource, or tool is implemented. Requires a dedicated Safety Lane plan after Phase 14 review."

[verification]
latest_commit = "d884813"
backend_pytest = 848
ruff = "clean"
migration_head = "0027"
