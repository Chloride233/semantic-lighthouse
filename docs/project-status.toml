# Semantic Lighthouse — Canonical Project Status
# This is the single source of truth for current product state.
# All entry documents MUST reference this file instead of maintaining
# their own "current phase" / "next phase" declarations.
# Schema version controls forward compatibility of check_doc_alignment.py.

[schema]
version = 1

[product]
phase = "Phase 16.2"
status = "delivered"
summary = "Pilot Outcome & FDE Delivery Record v1 — Phase 16.1 (PilotOutcomeRecord model + outcomes CR API) + 16.2 (outcome-summary aggregation endpoint) delivered. 53 tests pass, ruff clean, migration 0027 at head. Slices 16.3–16.4 pending."

[frontend]
status = "F2 complete"
summary = "Guided Pilot workspace delivered. F2A: Pilot entry + nav restructure. F2B: Model → Validate → Pilot full operation loop. F2C: Responsive, accessibility, visual polish. Phase 15.3 Slice B adds evidence-to-draft proposal UI in goal stage."
latest_verified_ui = "53/53"
latest_verified_e2e = "18/18"
screenshots_path = ".tmp/f2c/"

[focus]
current = "Phase 16.2 delivered"
description = "Phase 16.2 outcome-summary endpoint delivered: GET /groups/{gid}/projects/{pid}/outcome-summary aggregates project, latest outcome, evidence/package/runtime summaries. Member+ read, no side effects, no forbidden keys. 13 new tests (53 total pass). No new tables/migrations."

[next_decision_gate]
name = "implement Phase 16.3 or 16.4"
description = "Phase 16.1–16.2 delivered. Next decision: implement 16.3 (delivery report UI), 16.4 (exportable artifact), or review with project owner. See docs/phase16-planning.md."

[mcp]
runtime_status = "not started"
summary = "MCP is a future Agent-facing adapter candidate only. No MCP server/client, SDK, dependency, resource, or tool is implemented. Requires a dedicated Safety Lane plan after Phase 14 review."

[verification]
latest_commit = "58620bc"
backend_pytest = 848
ruff = "clean"
migration_head = "0027"
