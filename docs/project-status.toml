# Semantic Lighthouse — Canonical Project Status
# This is the single source of truth for current product state.
# All entry documents MUST reference this file instead of maintaining
# their own "current phase" / "next phase" declarations.
# Schema version controls forward compatibility of check_doc_alignment.py.

[schema]
version = 1

[product]
phase = "Phase 16.1"
status = "delivered"
summary = "Pilot Outcome & FDE Delivery Record v1 — Phase 16.1 backend foundation delivered: PilotOutcomeRecord model, migration 0027, CR API (no PATCH/DELETE), evidence/package/query_refs validation with bounded provenance, forbidden key rejection. 40 tests pass, ruff clean. Slices 16.2–16.4 pending."

[frontend]
status = "F2 complete"
summary = "Guided Pilot workspace delivered. F2A: Pilot entry + nav restructure. F2B: Model → Validate → Pilot full operation loop. F2C: Responsive, accessibility, visual polish. Phase 15.3 Slice B adds evidence-to-draft proposal UI in goal stage."
latest_verified_ui = "53/53"
latest_verified_e2e = "18/18"
screenshots_path = ".tmp/f2c/"

[focus]
current = "Phase 16.1 delivered"
description = "Phase 16.1 PilotOutcomeRecord backend delivered: model + migration 0027 + outcomes router + 40 tests. Owner/admin create immutable delivery records; member+ read/list. Evidence/package refs validated with bounded provenance. query_refs validated for forbidden keys. No frontend, no PATCH/DELETE."

[next_decision_gate]
name = "implement Phase 16.2 or 16.3"
description = "Phase 16.1 delivered. Next decision: implement 16.2 (read-only outcome summary endpoint), 16.3 (delivery report UI), or 16.4 (exportable interview artifact). See docs/phase16-planning.md."

[mcp]
runtime_status = "not started"
summary = "MCP is a future Agent-facing adapter candidate only. No MCP server/client, SDK, dependency, resource, or tool is implemented. Requires a dedicated Safety Lane plan after Phase 14 review."

[verification]
latest_commit = "58620bc"
backend_pytest = 848
ruff = "clean"
migration_head = "0027"
